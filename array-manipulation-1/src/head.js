/* exported head */
// Define a function that accepts an array
// and returns the first element.
// define a function named head
//  with an array parameter named array
// return the first element of the array
//    return the element at index 0 of the array

function head(array) {
  return array[0];
}
