/* exported getDescriptionOfPerson */
function getDescriptionOfPerson(p) {
  var descriptionOfPerson = p.name + ' is a ' + p.occupation + ' from ' + p.birthPlace + '.';
  return descriptionOfPerson;
}
