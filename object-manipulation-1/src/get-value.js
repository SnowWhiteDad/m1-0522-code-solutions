/* exported getValue */
// Define a function named getValue that takes an object and a key as arguments.
// The function should return the value of object found at the key property.
//    return object[key];

function getValue(object, key) {
  return object[key];
}
