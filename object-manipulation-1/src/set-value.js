/* exported setValue */
// Define a function named setValue
// that takes an object, a key, and a value as arguments.
// The function should set the value of object at the key property to the value argument.
//    object[key] = value;
// no return value

function setValue(object, key, value) {
  object[key] = value;
}
